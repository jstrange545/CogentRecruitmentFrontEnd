import { ApplicationEvaluation } from './application-evaluation';

describe('ApplicationEvaluation', () => {
  it('should create an instance', () => {
    expect(new ApplicationEvaluation()).toBeTruthy();
  });
});
