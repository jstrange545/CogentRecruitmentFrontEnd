import { InterviewEvaluation } from './interview-evaluation';

describe('InterviewEvaluation', () => {
  it('should create an instance', () => {
    expect(new InterviewEvaluation()).toBeTruthy();
  });
});
